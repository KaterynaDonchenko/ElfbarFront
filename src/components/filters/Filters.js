import FilterSlider from '../sliders/filterSlider/FilterSlider';

import './filters.scss';

const Filters = () => {
    return (
        <div className="filters">
            <FilterSlider/>
        </div>
    )
}

export default Filters;